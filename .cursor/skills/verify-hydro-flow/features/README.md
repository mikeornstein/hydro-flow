# Feature map

Index of what an agent can prove today. Add a file when a user-facing feature ships.

| Feature | File | Proof |
|---|---|---|
| Project files validate | [project-files.md](./project-files.md) | `npm run check` reports each `examples/*.hydroflow.json` matches the schema |
| Golden fixtures | [golden-fixtures.md](./golden-fixtures.md) | `npm run check` resolves every golden `file` and expected ids |
